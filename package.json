{
  "name": "foxpic",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "node build.js",
    "dev": "node build.js && wrangler pages dev dist",
    "deploy": "node build.js && wrangler pages deploy dist --project-name=foxpic"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
